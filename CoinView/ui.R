
library(shiny)
library(plotly)
library(quantmod)

# Define UI for application
shinyUI(fluidPage(
  tags$head(
    tags$link(rel = "stylesheet", type = "text/css", href = "styles.css")
  ),
  
  div(class = ''
  ),
  
  div(),
  div(),
  div(),
  div(),
  div()
  
))
